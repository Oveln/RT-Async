[package]
name = "riscv64-rt"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "riscv64_rt"
path = "src/lib.rs"

[dependencies]
riscv = { version = "0.16.0", default-features = false, features = ["critical-section-single-hart"] }

[features]
default = []
clear_bss = []
