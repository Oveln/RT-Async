[package]
name = "platform"
version = "0.1.0"
edition = "2024"

[dependencies]
riscv64-rt = { path = "archs/riscv64-rt" }
platform-traits = { path = "../platform-traits" }
qemu-virt = { path = "chips/qemu-virt" }
log = { version = "0.4", default-features = false }

[features]
